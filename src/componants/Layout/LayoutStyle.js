import {StyleSheet} from 'react-native';

const LayoutStyle = StyleSheet.create({
  container: {
    display: 'flex',
    flexDirection: 'row',
    backgroundColor: '#fff',
    color: 'black',
  },
  text: {
    color: '#000',
    fontSize: 16,
  },
});
export default LayoutStyle;
